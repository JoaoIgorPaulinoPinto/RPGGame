using UnityEngine;

public class InventorySlotInfo : MonoBehaviour {

    public InventorySlotType slotType;

  /*  private void Update()
    {
         if(slotType != InventorySlotType.defaultType)
         {
            
            transform.GetChild(0).GetComponent<InventorySlot>().labelName.gameObject.SetActive(false);
            transform.GetChild(0).GetComponent<InventorySlot>().labelQuant.gameObject.SetActive(false);
         }
        else
        {
            transform.GetChild(0).GetComponent<InventorySlot>().labelName.gameObject.SetActive(true);
            transform.GetChild(0).GetComponent<InventorySlot>().labelQuant.gameObject.SetActive(true);
        }

    }*/
}
